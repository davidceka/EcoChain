<section class="vh-100" >
    <div class="row d-flex justify-content-center align-items-center h-250 mt-5">
        <div class="col-md-8 col-lg-6 col-xl-4 offset-xl-1 mt-5" >
                
                <form action="/transactions/listrawmaterials" method="post">
                    <label for="selectWA">Choose a producer:</label>
                    <select id="selectWA" name="selectWA">   <!--> size="3"><-->
                        {{#each producers}}
                            <option value={{this.walletAddress}}>{{this.nome}}&nbsp;{{this.cognome}}</option>
                        {{/each}}
                    </select> <br><br>
                    <button type="submit">Confirm</button>
                </form>

                <ul data-role="listview">
                        {{#each rawMaterials}}
                            <form action="/transactions/acquistoMateriaPrima" method="post">
                                <li>
                                    <div class="card mb-3">
                                        <div class="card-body">
                                            <div class="row">
                                                <div class="col-sm-3">
                                                    <select id="_lottoScelto" name="_lottoScelto" hidden="true">
                                                        <option value={{this.idLotto}}></option>
                                                    </select>
                                                    <select id="_walletProduttore" name="_walletProduttore" hidden="true">
                                                        <option value={{this.owner}}></option>
                                                    </select>
                                                    <h6 value={{this.idLotto}} class="mb-0">Numero Lotto {{this.idLotto}}</h6>
                                                    <h6 class="mb-0">Nome {{this.nome}}</h6>
                                                    <h6 class="mb-0">Quantità {{this.quantita}}</h6>
                                                    <div class="col-sm-9 text-secondary">
                                                        <button  type="submit" class="btn btn-info " target="__blank">Compra</a>
                                                    </div><-->
                                                </div>
                                            
                                            </div>
                                        </div>
                                    </div>
                                </li>
                            </form>
                        {{/each}}
                   
                </ul>
                <hr class="dropdown-divider">
            </ul>
        </div>
    </div>
</section> 



